# CLAUDE.md — Custodly

See `AGENTS.md` in this same folder — it's the shared instructions file
for any agent working in this repo, Claude included. Read it before
touching `.env`, `.git-credentials`, or any secret on this machine.

Everything else project-specific lives in `docs/` and the Claude project
docs (design brief, boundary contract, threat model, MVP scope, license).
